telegram.boot.token=${TELEGRAM_TOKEN}
telegram.boot.name=${TELEGRAM_BOT}
admin.chat.id=${ADMIN_USER_BOT}
group.chat.id=${GROUP_USER_BOT}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
fpl.finalCookie=${FPL_COOKIE}
fpl.username=${FPL_USERNAME}
fpl.password=${FPL_PASSWORD}
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
hibernate.enable_lazy_load_no_trans=true
#
#spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.main.allow-bean-definition-overriding=true
logging.level.org.springframework.security=INFO
logging.level.svf.fpl.edu.bot=INFO
spring.thymeleaf.cache=false
server.session.tracking-modes=cookie
#custom properties
injury.time=${INJURY_CRON}